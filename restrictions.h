#ifndef RESTRICTIONS_H_INCLUDED
#define RESTRICTIONS_H_INCLUDED

#include<stdio.h>
#include"conio2.h"

bool restrictionOne(enum element **board, int X, int Y, int toInput);

bool restrictionTwo(enum element **board, int X, int Y, int toInput);

bool restrictionTree(enum element **board, int X, int Y, int toInput);

#endif // RESTRICTIONS_H_INCLUDED
